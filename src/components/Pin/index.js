import React from 'react'
import { Wrapper, Container } from "./pinStyles";
const Pin = () => {
    return (
        <Wrapper>
           <Container></Container> 
        </Wrapper>
    )
}

export default Pin
